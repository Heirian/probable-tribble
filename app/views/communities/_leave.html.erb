<%= form_for(membership(current_profile, @community),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= button_tag type: 'submit', class: "btn btn-secondary joined-community p-2", title: t(:joined) do %>
    <i class="fas fa-check-square"></i> <%= t(:joined) %>
  <% end %>
<% end %>

<%= javascript_include_tag 'community/hover_button_transition' %>

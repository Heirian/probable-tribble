<%= form_for(current_profile.memberships.build, remote: true) do |f| %>
  <%= hidden_field_tag :community_id, @community.id %>
  <%= button_tag type: 'submit', class: "btn btn-primary p-2", title: t(:join, count: 1)  do %>
    <i class="fa fa-crosshairs" aria-hidden="true"></i> <%= t(:join, count: 1) %>
  <% end %>
<% end %>
